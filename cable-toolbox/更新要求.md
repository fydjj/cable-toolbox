# 更新要求
- ## 功能：
	- 每组新增一个输入值，为接触阻抗，这个输入也可开关，总阻抗输出时加上接触阻抗，若接触阻抗开关关掉则为0，若打开则可输入
		- 若接触阻抗开关打开：VBUS默认为10毫欧，GND默认为10毫欧，内层缠绕默认为35毫欧
	- 新增第三行，第三行输出GND和缠绕各自加上各自的接触阻抗，再并联后的值。
		- 若无外层缠绕，输出GND和内层缠绕各自加上各自的接触阻抗，再并联后的值。
		- 若有外层缠绕，关闭内层和外层缠绕的接触阻抗输入，在第三行中加入“缠绕接触阻抗”，该项可输入，默认为35毫欧，
- UI界面：
  - 压缩每行的高度，兼容手机显示